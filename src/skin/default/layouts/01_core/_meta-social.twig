
{% set meta = sprout.social.meta.get() %}

{% for name, value in meta.properties ?? [] %}
    <meta property="{{ name }}" content="{{ value }}"/>
{% endfor %}

{% for name, value in meta.names ?? [] %}
    <meta name="{{ name }}" content="{{ value }}"/>
{% endfor %}
